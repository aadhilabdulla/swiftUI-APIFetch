# API Fetch using SwiftUI

An app built using **SwiftUI** that demonstrates how to call an API and display the retrieved data using the **MVVM (Model-View-ViewModel)** architecture.  
This project focuses on clean code structure, data binding, and reactive UI updates.
